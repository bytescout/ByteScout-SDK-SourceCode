---
templateVersion: 1
templatePriority: 1
sourceId: Zendesk
detectionRules:
  keywords:
  - Zendesk, Inc.
  - Invoice
  - 519184
fields:
  invoiceId:
    regex: Invoice Number:\s+(INV\d+)
  dateIssued:
    regex: Invoice Date:\s+([a-zA-Z]+ \d+, \d{4})
    type: dateTime
    dateFormat: MMMM d, yyyy
  dateDue:
    regex: Due Date:\s+([a-zA-Z]+ \d+, \d{4})
    type: dateTime
    dateFormat: MMMM d, yyyy
  total:
    regex: Total:\s+\$(\d+\.\d\d)
    type: decimal
  subTotal:
    regex: Subtotal:\s+\$(\d+\.\d\d)
    type: decimal
  tax:
    regex: Tax:\s+\$(\d+\.\d\d)
    type: decimal
  sourceName:
    static: Zendesk, Inc.
  sourceWebsite:
    static: zendesk.com
  sourceEmail:
    static: ar@zendesk.com
  currency:
    static: USD
